{
  "name": "hookhub-bootstrap-local",
  "version": "0.1.0",
  "description": "HookHub bootstrap module for local configuration files",
  "scripts": {
    "test": "echo \"Error: no tests yet\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "js-yaml": "^3.13.0"
  },
  "devDependencies": {
    "standard": "^12.0.1"
  }
}
